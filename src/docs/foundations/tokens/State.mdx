import { Meta, ColorPalette, ColorItem } from '@storybook/blocks';
import { stateColorGroups } from './lib/colors';

<Meta title="Foundations/Tokens/State" />

# State

<ColorPalette>
  {stateColorGroups.map((group) => (
    <ColorItem
      key={group.name}
      title={group.name}
      subtitle={group.subtitle}
      colors={group.colors}
    />
  ))}
</ColorPalette>
