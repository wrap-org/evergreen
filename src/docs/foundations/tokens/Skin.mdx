import { Meta, ColorPalette, ColorItem } from '@storybook/blocks';
import { skinColorGroups } from './lib/colors';

<Meta title="Foundations/Tokens/Skin" />

# Skin

<ColorPalette>
  {skinColorGroups.map((group) => (
    <ColorItem
      key={group.name}
      title={group.name}
      subtitle={group.subtitle}
      colors={group.colors}
    />
  ))}
</ColorPalette>
