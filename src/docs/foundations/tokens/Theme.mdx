import { Meta } from '@storybook/addon-docs/blocks';
import { Tokens } from '@etchteam/storybook-addon-design-token-tables';

<Meta title="Foundations/Tokens/Theme" />

# Theme

> Collections of token values grouped to create consistent visual modes.

Themes are collections of token values grouped to create consistent light, dark,
or brand-specific visual modes. Themes allow for comprehensive styling changes by
modifying token values while preserving the same underlying component structure.

<Tokens collection="evg-theme" />
