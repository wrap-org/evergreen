import { Meta, ColorPalette, ColorItem } from '@storybook/blocks';
import { buttonColorGroups } from './lib/colors';

<Meta title="Foundations/Tokens/Button" />

# Button

<ColorPalette>
  {buttonColorGroups.map((group) => (
    <ColorItem
      key={group.name}
      title={group.name}
      subtitle={group.subtitle}
      colors={group.colors}
    />
  ))}
</ColorPalette>
