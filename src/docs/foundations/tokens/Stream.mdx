import { Meta, ColorPalette, ColorItem } from '@storybook/blocks';
import { streamColorGroups } from './lib/colors';

<Meta title="Foundations/Tokens/Stream" />

# Stream

<ColorPalette>
  {streamColorGroups.map((group) => (
    <ColorItem
      key={group.name}
      title={group.name}
      subtitle={group.subtitle}
      colors={group.colors}
    />
  ))}
</ColorPalette>
