import { Meta, ColorPalette, ColorItem, Typeset } from '@storybook/addon-docs/blocks';

<Meta title="Foundations/Typography" />

# Typography

> A hierarchical system balancing readability and aesthetics.

Hierarchical system of font families, weights, sizes, and line heights balances readability and aesthetics.
Evergreen's typography foundation establishes a clear hierarchy and provides guidance for headings, body
text, and specialised text elements.

## Display

<Typeset
  fontSizes={[
    'var(--evg-font-size-display-lg)',
    'var(--evg-font-size-display-md)',
    'var(--evg-font-size-display-sm)',
  ]}
  fontWeight="var(--evg-font-weight-bold)"
  sampleText="The future is in our hands to shape."
  fontFamily="var(--evg-font-family-heading)"
/>

## Headings

<Typeset
  fontSizes={[
    'var(--evg-font-size-heading-xl)',
    'var(--evg-font-size-heading-lg)',
    'var(--evg-font-size-heading-md)',
    'var(--evg-font-size-heading-sm)',
    'var(--evg-font-size-heading-xs)',
    'var(--evg-font-size-heading-xxs)',
  ]}
  fontWeight="var(--evg-font-weight-bold)"
  sampleText="The future is in our hands to shape."
  fontFamily="var(--evg-font-family-heading)"
/>

# Body

<Typeset
  fontSizes={[
    'var(--evg-font-size-body-xl)',
    'var(--evg-font-size-body-lg)',
    'var(--evg-font-size-body-md)',
    'var(--evg-font-size-body-sm)',
    'var(--evg-font-size-body-xs)',
  ]}
  sampleText="Lorem ipsum dolor sit amet, consectetur adipiscing elit."
  fontFamily="var(--evg-font-family)"
/>
